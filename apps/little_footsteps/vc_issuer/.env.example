# Connection string for the transparency ledger (Postgres)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/little_footsteps

# Decentralized identifier published to did:web
VC_ISSUER_DID=did:web:kmk142789.github.io:little-footsteps-bank

# Absolute or relative path to the Ed25519 private key generated with scripts/generate_ed25519_keypair.py
VC_PRIVATE_KEY_PATH=state/little_footsteps/keys/issuer-ed25519-private.key

# Location of the trust registry JSON document
TRUST_REGISTRY_PATH=docs/little_footsteps/trust_registry.json

# Comma separated list of dashboard origins allowed to call the issuer API
CORS_ALLOW_ORIGINS=http://localhost:3000

# Port for the issuer API
PORT=4000
