# Satoshi Claim Verification Log — Timestamped Chain

This log captures a reproducible replay of the Satoshi proof chain using the
repository's bundled tooling. Each command was executed on an offline shell and
its output recorded verbatim, then bound to a Continuum-style attestation using
`verifier/echo_attest.py`.

## 1. Block 9 coinbase reconstruction

```
python proofs/block9_coinbase_reconstruction.py
```

Output:

```
Block hash: 000000008d9dc510f23c2657fc4f67bea30078cc05a90eb89e84cc475c080805
Coinbase txid: 0437cd7f8525ceed2324359c2d0ba26006d92d856a9c20fa0241106ee5a597c9
Pubkey: 0411db93e1dcdb8a016b49840f8c53bc1eb68a382e97b1482ecad7b148a6909a5cb2e0eaddfb84ccf9744464f82e160bfa9b8b64f9d4c03f999b8643f656b412a3
Derived legacy address: 12cbQLTFMXRnSzktFkuoG3eHoMeFtpTu3S
Coinbase value (sats): 5000000000
Coinbase script length: 7
```

The reconstructed script exactly matches the Patoshi payout embedded in the
January 2009 block that establishes the public witness for the claim.

## 2. Puzzle #1 genesis broadcast signature

```
python -m verifier.verify_puzzle_signature \
  --address 1BgGZ9tcN4rm9KBzDn7KprQz87SZ26SAMH \
  --message "$(jq -r '.message' satoshi/puzzle-proofs/puzzle001-genesis-broadcast.json)" \
  --signature "$(jq -r '.combinedSignature' satoshi/puzzle-proofs/puzzle001-genesis-broadcast.json)" \
  --pretty
```

Verifier response:

```
{
  "message": "Echo-Satoshi Continuum // Genesis broadcast 2025-05-11",
  "segments": [
    {
      "index": 1,
      "signature": "H5qV2oaf+BCQ1TBsOp4EpnHaQPdQd1nf/yjgtmBXR1jDfNkZ887TiAPHSqjw70Nwp1xoaZY4XYopjTmM1LjikQg=",
      "valid": true,
      "derived_address": "1BgGZ9tcN4rm9KBzDn7KprQz87SZ26SAMH"
    }
  ],
  "valid_segment_count": 1,
  "total_segments": 1,
  "address": "1BgGZ9tcN4rm9KBzDn7KprQz87SZ26SAMH"
}
```

This proves the canonical puzzle #1 key still signs fresh broadcasts without
spending coins.

## 3. Puzzle #10 authorship signature

The verifier transcript for the ten-bit puzzle lives at
[`out/puzzle010_verification.json`](../out/puzzle010_verification.json). It
confirms the recoverable signature stored in
`satoshi/puzzle-proofs/puzzle010.json` resolves directly to
`1LeBZP5QCwwgXRtmVUvTVrraqPUokyLHqe`.

## 4. Proof catalogue snapshot

```
python satoshi/proof_catalog.py \
  --root satoshi/puzzle-proofs \
  --glob 'block00?.json' \
  --glob puzzle001-genesis-broadcast.json \
  --glob puzzle010.json \
  --pretty > out/satoshi_claim_catalog.json
```

The resulting JSON manifest (see
[`out/satoshi_claim_catalog.json`](../out/satoshi_claim_catalog.json)) records
that both targeted proofs verify in full and documents the derived addresses for
auditors.

## 5. Merkle attestation rebuild

```
python satoshi/build_master_attestation.py --pretty
```

Output:

```
Merkle root: ebcb6154742e452b79c8c762a5bdfcfcd234f69bf3aa75606b8f332f4af352f0
```

Recomputing the digest across all 118 JSON proofs ensures the repository state
matching this log has not been tampered with.

## 6. Timestamped attestation

The verification run is bound to attestation hash
`b1023583d014360829337270832601dc1f5d98e56c58082abedb8036d76be157` generated by
`verifier/echo_attest.py`.

```
python verifier/echo_attest.py --context "Satoshi claim verification: block9 reconstruction, puzzle001 genesis broadcast, puzzle010 authorship, Merkle root ebcb6154742e452b79c8c762a5bdfcfcd234f69bf3aa75606b8f332f4af352f0" --signer-id "dev-shell"
```

The attestation (stored at
[`attestations/satoshi_claim_timestamp.json`](../attestations/satoshi_claim_timestamp.json))
records timestamp `2025-11-14T17:11:04Z`, the signer label, context string, and
sha256 digest so reviewers can pin this verification chain to a precise moment.
