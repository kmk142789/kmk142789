version: "3.9"
services:
  continuum:
    build: .
    image: echo/continuum-api:0.1.0
    ports: ["8000:8000"]
    environment:
      ECHO_LEDGER_DIR: /data/ledger
    volumes:
      - ./.attest:/data/ledger:ro
