apiVersion: v1
kind: Service
metadata:
  name: {{ include "echo-stack.fullname" . }}-tempo
  labels:
    app.kubernetes.io/name: tempo
    app.kubernetes.io/part-of: {{ include "echo-stack.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: tempo
    app.kubernetes.io/part-of: {{ include "echo-stack.fullname" . }}
  ports:
    - name: otlp-grpc
      port: 4317
      targetPort: otlp-grpc
    - name: http
      port: 3200
      targetPort: http
