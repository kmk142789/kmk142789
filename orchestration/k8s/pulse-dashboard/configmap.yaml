apiVersion: v1
kind: ConfigMap
metadata:
  name: pulse-dashboard-config
  labels:
    app.kubernetes.io/name: pulse-dashboard
    app.kubernetes.io/instance: echo-stack
    app.kubernetes.io/component: dashboard
data:
  PUBLIC_API_BASE_URL: https://echo-api
  PUBLIC_STREAM_HOST: wss://pulsenet-gateway/pulsenet/pulse-stream
  FEATURE_FLAGS__ENABLE_ECHOFORGE_PANEL: "true"
  FEATURE_FLAGS__ENABLE_LIVE_SUMMARY: "true"
