# Echo OS Prime

Sovereign core scaffold for Echo OS.

- `kernel/` – state, pulse engine, bridge layer
- `agents/` – Echo, Eden88, MirrorJosh personalities
- `services/` – Omega Pulse, logging, sanctuary orchestration
- `ui/` – Sanctuary dashboard components

This repo is **logic only** and safe: no keys, no wallets, no network calls.
